# doctor-mcp-cli v0.1.1

This release adds small but useful quality-of-life improvements for early users and CI workflows.

## Highlights

- Add `doctor-mcp --version`.
- Add `doctor-mcp --ci` for short warning/error-only logs.
- Add lightweight HTTP/SSE URL reachability checks.
- Add project config discovery for Windsurf, Cline, Roo Code, and Continue.
- Improve startup error messages for missing commands and network failures.
- Expand README with the new supported clients and checks.

## Install

```bash
npx doctor-mcp-cli
```

or:

```bash
npm install -g doctor-mcp-cli
doctor-mcp
```

## Links

- npm: https://www.npmjs.com/package/doctor-mcp-cli
- GitHub: https://github.com/LSUFN/doctor-mcp-cli
